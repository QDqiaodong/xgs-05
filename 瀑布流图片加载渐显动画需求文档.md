# 瀑布流作品图片加载渐显动画 — 需求文档

## 1. 需求概述

为手作空间（Handmade Studio）前端项目的瀑布流作品列表中的图片增加加载渐显动画效果，在图片加载完成前显示低饱和度占位色块和骨架轮廓，加载完成后平滑淡入，避免页面跳动，提升浏览体验的流畅感和高级感。

**涉及模块：** `WorkCard.vue` 组件（首页、分类页、收藏页共用）

---

## 2. 功能需求明细

### 2.1 加载占位阶段（图片未加载完成）

| 功能点 | 说明 | 实现方式 |
|--------|------|----------|
| 低饱和度占位色块 | 图片加载前显示柔和的灰色渐变背景，避免空白突兀感 | `linear-gradient(135deg, #f5f5f5 0%, #e8e8e8 100%)` |
| 骨架轮廓 Shimmer 动画 | 占位色块上覆盖流动的白色高光，给用户明确的"加载中"视觉反馈 | CSS `@keyframes shimmer` 动画，1.5s 周期，从左向右滑动 |
| 固定宽高比占位 | 容器预占正确的宽高比，防止图片加载后瀑布流布局跳动 | CSS `aspect-ratio` 属性，从 URL 解析尺寸，默认 3:4 |

### 2.2 图片加载阶段

| 功能点 | 说明 | 实现方式 |
|--------|------|----------|
| 平滑淡入动画 | 图片加载完成后从透明平滑过渡到完全可见 | `transition: opacity 0.6s ease-out` |
| 缓存图片兼容 | 已缓存的图片（`img.complete === true`）也能正确触发淡入 | `onMounted` 中检查 `complete` 状态并设置 `imgLoaded` |
| 加载错误处理 | 图片加载失败时显示友好的错误提示，而非破图 | `@error` 事件 → 显示带 `PictureFilled` 图标的"加载失败"占位 |

### 2.3 已有功能保留

- 鼠标悬停时图片放大（`transform: scale(1.05)`）
- 卡片上浮阴影效果
- "热门"标签显示
- 点击跳转详情页
- 收藏功能

---

## 3. 技术实现说明

### 3.1 文件变更

**修改文件：** `frontend/src/components/WorkCard.vue`

### 3.2 核心实现逻辑

#### 3.2.1 状态管理

```javascript
const imgLoaded = ref(false)   // 图片是否加载完成
const imgError = ref(false)    // 图片是否加载失败
const imgRef = ref(null)       // img 元素 DOM 引用
```

#### 3.2.2 模板结构

```html
<div class="work-image" :style="{ aspectRatio: imageRatio }">
  <!-- 1. 占位骨架（未加载且未失败时显示） -->
  <div v-if="!imgLoaded && !imgError" class="image-placeholder">
    <div class="skeleton-shimmer"></div>
  </div>

  <!-- 2. 主图（始终在 DOM 中，通过 opacity 控制可见性） -->
  <img
    ref="imgRef"
    :src="work.coverImage || fallbackUrl"
    :class="{ 'img-loaded': imgLoaded, 'img-hidden': !imgLoaded }"
    @load="onImgLoad"
    @error="onImgError"
  />

  <!-- 3. 错误占位（加载失败时显示） -->
  <div v-if="imgError" class="image-error">...</div>
</div>
```

> ⚠️ **关键设计：** `<img>` 标签**始终渲染在 DOM 中**，仅通过 CSS `opacity` 控制可见性。不能使用 `v-show` 或 `v-if` 控制 `<img>` 的存在，否则浏览器不会加载 `display: none` 的图片，导致 `@load` 事件永不触发，形成死循环。

#### 3.2.3 宽高比计算

```javascript
const imageRatio = computed(() => {
  const url = props.work.coverImage || ''
  const match = url.match(/\/(\d+)\/(\d+)/)  // 匹配 picsum.photos/300/400 格式
  if (match) {
    const w = parseInt(match[1])
    const h = parseInt(match[2])
    if (w && h) return `${w} / ${h}`
  }
  return '3 / 4'  // 默认比例
})
```

#### 3.2.4 缓存图片兼容处理

```javascript
onMounted(() => {
  nextTick(() => {
    if (imgRef.value && imgRef.value.complete) {
      if (imgRef.value.naturalWidth === 0) {
        imgError.value = true    // 已完成但无宽度 = 加载失败
      } else {
        imgLoaded.value = true   // 已缓存，直接标记已加载
      }
    }
  })
})
```

#### 3.2.5 CSS 动画

```scss
// Shimmer 骨架流动动画
@keyframes shimmer {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

.skeleton-shimmer {
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(255, 255, 255, 0.5) 50%,
    transparent 100%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}

// 图片淡入动画
.work-image img {
  transition: opacity 0.6s ease-out, transform 0.5s;
}
.work-image img.img-hidden { opacity: 0; }
.work-image img.img-loaded { opacity: 1; }
```

---

## 4. Bug 修复记录

### 4.1 问题描述

首屏加载时，瀑布流作品图永远停留在占位骨架状态，主图永不显示。

### 4.2 根因分析

**第一版实现的错误代码：**

```html
<img
  v-show="imgLoaded && !imgError"   <!-- ❌ 错误！ -->
  :src="..."
  @load="onImgLoad"
/>
```

**死循环形成过程：**

1. 初始状态 `imgLoaded = false`
2. `v-show` 将 `<img>` 设为 `display: none`
3. 浏览器**不会加载** `display: none` 的图片资源
4. 图片不加载 → `@load` 事件永不触发
5. `@load` 不触发 → `imgLoaded` 永远为 `false`
6. 回到步骤 2，无限循环

### 4.3 修复方案

| 修复项 | 修改前 | 修改后 |
|--------|--------|--------|
| img 显隐控制 | `v-show="imgLoaded && !imgError"` | 移除 `v-show`，改用 CSS class 控制 `opacity` |
| 初始透明度 | `opacity: 0`（直接写在 img 样式上） | `.img-hidden { opacity: 0 }` + `.img-loaded { opacity: 1 }` |
| 缓存兼容 | 无处理 | `onMounted` 中检查 `img.complete` 并设置对应状态 |

### 4.4 验证结果

通过浏览器 DOM 检查，所有 8 张作品图片状态正常：
- `complete: true` — 加载完成
- `naturalWidth: 300` — 实际宽度正确
- `display: "block"` — 正常渲染
- `opacity: "1"` — 完全可见
- `classList: "img-loaded"` — 状态类名正确

---

## 5. 需求验收清单

| 编号 | 验收项 | 验收标准 | 状态 |
|------|--------|----------|------|
| 1 | 低饱和度占位色块 | 图片加载前显示柔和灰色渐变背景，不突兀 | ✅ |
| 2 | 骨架 Shimmer 动画 | 占位区域有流动高光效果，1.5s 周期平滑 | ✅ |
| 3 | 图片平滑淡入 | 加载完成后 opacity 从 0 → 1 过渡（0.6s ease-out） | ✅ |
| 4 | 无页面跳动 | 图片加载前后瀑布流布局稳定，无位移 | ✅ |
| 5 | 首屏图片正常显示 | 首屏刷新后图片能正常进入加载态并最终显示 | ✅（Bug 已修复） |
| 6 | 缓存图片兼容 | 已缓存图片不卡在占位状态，直接显示 | ✅ |
| 7 | 加载失败处理 | 图片 404/失败时显示友好提示而非破图 | ✅ |
| 8 | 构建通过 | `npm run build` 无错误 | ✅ |
| 9 | 现有功能保留 | 悬停放大、收藏、跳转等功能不受影响 | ✅ |

---

## 6. 影响范围

| 页面 | 使用 WorkCard | 受影响 |
|------|---------------|--------|
| 首页（热门作品瀑布流） | ✅ | ✅ |
| 分类页（分类作品瀑布流） | ✅ | ✅ |
| 收藏页（我的收藏瀑布流） | ✅ | ✅ |
| 作品详情页 | ❌ | ❌ |
| 个人中心页 | ❌ | ❌ |
| 发布作品页 | ❌ | ❌ |

---

## 7. 后续优化建议

1. **渐进式加载（Progressive Loading）**：可接入 LQIP（Low Quality Image Placeholders）或 SQIP（SVG-based）技术，先加载超模糊缩略图再过渡到高清图
2. **IntersectionObserver**：结合 `loading="lazy"`，对进入视口的图片优先加载
3. **图片 CDN 优化**：为不同设备尺寸返回适配分辨率的图片（`srcset` + `sizes`）
4. **骨架内容匹配**：占位色块可根据作品分类使用不同的低饱和度色调
