# 发布作品用料智能联想提示功能 - 详细介绍文档

## 一、功能概述

本功能在「发布手作作品」页面的用料输入框中增加智能联想提示能力，根据平台历史用料数据和用户已输入内容进行下拉推荐，支持快捷选中填入和批量添加，旨在提升创作者发布作品的效率。

---

## 二、问题背景

### 原有方案
- 用料清单为纯 `textarea` 文本框
- 用户需手动输入全部用料名称
- 无任何辅助输入机制，重复输入效率低
- 不同创作者对同一种用料的命名不统一（如"毛线"、"羊毛线"）

### 改造目标
1. **智能联想**：根据已输入字符实时推荐历史用料
2. **快速填充**：支持鼠标/键盘快捷选中填入
3. **批量添加**：提供热门用料一键批量添加
4. **标准化**：引导创作者使用统一的用料命名

---

## 三、架构设计

### 3.1 整体流程图

```
用户输入关键词
      │
      ▼
前端 el-autocomplete 触发 fetchSuggestions
      │
      ▼
GET /api/work/materials/suggest?keyword=xxx&limit=10
      │
      ▼
后端 WorkService.suggestMaterials()
      │
      ├─► 命中 Redis 缓存？ ──是──► 返回缓存数据
      │                           │
      │                          否
      │                           │
      ▼                           ▼
从 DB 加载所有作品 → 解析 materials 字段 → 统计词频
      │
      ▼
按关键词过滤 + 按频率排序
      │
      ▼
写入 Redis 缓存（TTL 2小时）
      │
      ▼
返回 JSON: [{ name: "纯羊毛线500g", count: 5 }, ...]
      │
      ▼
前端渲染下拉列表（显示用料名 + 使用人数）
      │
      ▼
用户点击选中 / 回车确认
      │
      ▼
加入 materialTags[] 数组（Tag 形式展示）
      │
      ▼
watch 自动同步 → form.materials = tags.join('，')
      │
      ▼
handleSubmit() 时再次从 tags 拼接，双保险写入 payload
```

### 3.2 模块划分

| 模块 | 技术栈 | 职责 |
|------|--------|------|
| 后端 Controller | Spring Boot | 提供 REST API 入口 |
| 后端 Service | MyBatis-Plus + Redis | 用料数据提取、统计、缓存、推荐 |
| 前端输入组件 | Element Plus el-autocomplete | 关键词输入、联想下拉展示 |
| 前端标签管理 | Element Plus el-tag | 已选用料展示、单独删除 |
| 前端热门区域 | Element Plus el-tag + 自定义样式 | 热门用料快速切换、批量操作 |

---

## 四、后端实现详解

### 4.1 API 定义

**接口地址**：`GET /api/work/materials/suggest`

**请求参数**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `keyword` | String | 否 | `null` | 搜索关键词，为空时返回热门用料 |
| `limit` | Integer | 否 | `10` | 返回条目数上限 |

**响应示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    { "name": "纯羊毛线500g", "count": 5 },
    { "name": "棒针一副", "count": 5 },
    { "name": "钩针", "count": 4 },
    { "name": "陶土500g", "count": 4 }
  ]
}
```

**代码位置**：
- Controller: [WorkController.java](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/backend/src/main/java/com/handmade/controller/WorkController.java#L53-L58)
- Service 接口: [WorkService.java](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/backend/src/main/java/com/handmade/service/WorkService.java#L21)
- Service 实现: [WorkServiceImpl.java](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/backend/src/main/java/com/handmade/service/impl/WorkServiceImpl.java#L98-L221)

### 4.2 核心算法

#### 用料提取与分词
从 `work` 表的 `materials` 字段（如 `"纯羊毛线500g，棒针一副，钩针"`）中解析独立用料：

```java
// 支持中英文逗号、顿号、分号、换行作为分隔符
private static final Pattern MATERIAL_SPLIT_PATTERN = Pattern.compile("[,，、;；\\n\\r]+");

String[] parts = MATERIAL_SPLIT_PATTERN.split(materials);
for (String part : parts) {
    String trimmed = part.trim();
    // 过滤过短（<2字符）或过长（>50字符）的无效项
    if (trimmed.length() >= 2 && trimmed.length() <= 50) {
        countMap.merge(trimmed, 1, Integer::sum);
    }
}
```

#### 推荐排序策略
排序优先级从高到低：
1. **使用频率降序**：越多人使用越靠前
2. **前缀匹配优先**：以关键词开头的排在包含匹配前面
3. **字典序升序**：频率相同时按名称排序

```java
.sorted((a, b) -> {
    int countCompare = b.getValue() - a.getValue();
    if (countCompare != 0) return countCompare;
    if (StringUtils.hasText(keyword)) {
        boolean aStartsWith = a.getKey().toLowerCase().startsWith(keyword.toLowerCase());
        boolean bStartsWith = b.getKey().toLowerCase().startsWith(keyword.toLowerCase());
        if (aStartsWith && !bStartsWith) return -1;
        if (!aStartsWith && bStartsWith) return 1;
    }
    return a.getKey().compareTo(b.getKey());
})
```

#### 冷启动默认数据
当数据库中历史作品较少时，内置 20 个常见手作用料作为推荐基础：

```java
String[][] defaults = {
    {"纯羊毛线500g", "5"}, {"棒针一副", "5"}, {"钩针", "4"},
    {"亚克力毛线", "4"}, {"棉花", "4"}, {"陶土500g", "4"},
    // ... 共 20 项
};
```

#### Redis 缓存
- **Key**：`materials:suggest`
- **TTL**：2 小时
- **内容**：Top 100 热门用料（name + count）
- 避免每次请求都扫描全表解析，显著提升性能

---

## 五、前端实现详解

### 5.1 组件结构

```
用料清单（el-form-item）
└── materials-editor（容器）
    ├── materials-tags（已选标签区）
    │   └── el-tag × N（每个用料一个标签，带关闭按钮）
    ├── el-autocomplete（智能输入框）
    │   └── 下拉推荐项：用料名 + "已有 X 人使用"
    ├── materials-hot（热门用料区）
    │   ├── 标题：📈 热门用料（点击快速添加）
    │   └── el-tag × 12（点击切换添加/取消，已添加显示 ✓）
    └── materials-batch（批量操作区）
        ├── 一键添加全部热门用料
        └── 清空所有用料（带确认弹窗）
```

**代码位置**：[PublishView.vue](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/frontend/src/views/PublishView.vue)

### 5.2 响应式状态

```javascript
const materialInput = ref('')           // 输入框当前内容
const materialTags = ref([])            // 已选用料标签数组
const hotMaterials = ref([])            // 热门用料列表（从后端拉取）
const suggestCache = ref({})            // 前端联想结果缓存
```

### 5.3 数据同步机制（双保险）

#### 第一层：watch 实时同步
标签变化时自动同步到 `form.materials`：

```javascript
watch(materialTags, (val) => {
  form.materials = val.join('，')
}, { immediate: true })
```

#### 第二层：提交时重新拼接（本次修复的核心）
在 `handleSubmit` 中从 `materialTags` 重新生成字符串，直接写入 payload，彻底避免数据丢失：

```javascript
async function handleSubmit() {
  // ... 表单校验 ...

  // ✅ 关键修复：直接从标签数组拼接，不依赖 form.materials 的中间状态
  const materialsValue = materialTags.value.join('，')
  form.materials = materialsValue

  const payload = {
    // ...
    materials: materialsValue,  // 直接使用拼接后的值
    // ...
  }

  console.log('[发布作品] 用料 tags:', materialTags.value)
  console.log('[发布作品] 用料字符串:', materialsValue)
}
```

#### resetForm 同步修复
重置表单时显式清空 `form.materials`，避免 `resetFields()` 回滚导致的不一致：

```javascript
function resetForm() {
  formRef.value.resetFields()
  materialTags.value = []
  materialInput.value = ''
  form.materials = ''   // ✅ 显式清空
  form.steps = [createEmptyStep()]
}
```

### 5.4 交互细节

#### 智能联想
- 输入框获得焦点时展示热门推荐（无需输入）
- 输入时调用后端接口，前端做结果缓存（相同关键词不重复请求）
- 下拉项显示「用料名称」+「已有 X 人使用」，帮助用户决策

#### 多种添加方式
| 操作方式 | 说明 |
|----------|------|
| 点击下拉项 | 选中联想推荐项直接加入标签 |
| 回车键 | 将输入框内容按逗号/顿号/分号分隔后批量加入 |
| 点击热门标签 | 单个热门用料快速切换添加/取消 |
| 「一键添加全部热门用料」按钮 | 将所有未添加的热门用料批量加入 |

#### 防重复机制
```javascript
function addMaterialTag(name) {
  const trimmed = (name || '').trim()
  if (!trimmed) return
  if (materialTags.value.includes(trimmed)) {
    ElMessage.warning('该用料已添加')   // 重复时提示
    return
  }
  materialTags.value.push(trimmed)
}
```

---

## 六、样式设计

### 6.1 视觉层次
| 区域 | 样式 | 说明 |
|------|------|------|
| 已选标签 | `el-tag type="primary" effect="light"` | 蓝色实心标签，突出已选 |
| 热门用料区 | 浅蓝色渐变背景 + 蓝色边框 | 视觉上独立于输入区 |
| 未添加热门标签 | `type="success" effect="plain"` | 绿色描边，暗示可点击添加 |
| 已添加热门标签 | `type="info" effect="plain"` + ✓ | 灰色描边 + 勾选图标，表示已选中 |
| 批量按钮 | 左主（primary）右次（default） | 主次分明 |

### 6.2 微交互
- 热门标签 hover 时轻微上浮（`translateY(-1px)`）
- 联想下拉项 hover 时浅灰背景
- 所有状态变化均有 ElMessage 提示

---

## 七、数据库兼容性

### 字段使用
完全复用现有 `work.materials` 字段（`VARCHAR(500)`），无需修改表结构。

### 存储格式
标签数组用中文逗号 `，` 拼接成字符串存储，与原有格式完全一致：

```
materialTags: ["纯羊毛线500g", "棒针一副", "钩针"]
        ↓ join('，')
form.materials: "纯羊毛线500g，棒针一副，钩针"
```

这保证了：
- 旧作品数据可正常展示
- 新提交数据与旧数据格式统一
- 作品详情页无需改造即可展示用料

---

## 八、变更文件清单

### 后端（3 个文件）

| 文件 | 变更说明 |
|------|----------|
| [WorkController.java](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/backend/src/main/java/com/handmade/controller/WorkController.java) | 新增 `GET /materials/suggest` 接口 |
| [WorkService.java](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/backend/src/main/java/com/handmade/service/WorkService.java) | 新增 `suggestMaterials` 方法签名 |
| [WorkServiceImpl.java](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/backend/src/main/java/com/handmade/service/impl/WorkServiceImpl.java) | 实现用料提取、统计、排序、缓存、推荐的完整逻辑 |

### 前端（1 个文件）

| 文件 | 变更说明 |
|------|----------|
| [PublishView.vue](file:///Users/Admin/Desktop/solo-0601/xgs-0601/xgs-05/frontend/src/views/PublishView.vue) | 1. 用料 textarea → 智能标签编辑器<br>2. 新增 watch 同步 + handleSubmit 双保险拼接<br>3. 修复 resetForm 清空 form.materials<br>4. 新增相关样式（scoped + 全局） |

---

## 九、修复记录

### Bug：发布作品请求体丢失 materials

**问题描述**：用料标签的交互操作没有可靠地写回最终提交的 payload 中。

**根本原因**：
1. 仅依赖 `watch` 同步到 `form.materials`，而 `formRef.resetFields()` 等操作可能将其重置
2. 提交时直接读取 `form.materials`，存在中间状态被意外修改的风险

**修复方案**：
1. 在 `handleSubmit` 中**直接从 `materialTags.value` 重新拼接** `materialsValue`，作为提交的唯一真源
2. 同步将拼接结果回写到 `form.materials`，保持状态一致
3. 在 `resetForm` 中显式将 `form.materials` 清空
4. 增加 `console.log` 调试日志，便于排查问题

---

## 十、后续优化建议

1. **用户个性化推荐**：结合用户历史发布记录，优先推荐该用户常用的用料
2. **分类关联推荐**：选择手作品类后，优先推荐该品类下的高频用料
3. **用料单位标准化**：将用料名与单位分离（如"羊毛线" + "500g"），支持自动补全单位
4. **同义词合并**：将"毛线"、"羊毛线"、"羊毛毛线"等同义词归并，减少推荐冗余
5. **用料图片**：为常用用料添加缩略图，在联想下拉中展示
